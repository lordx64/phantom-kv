"""Shared scoreboard: every graft arm (v1 prefill, v2 compiled soft prompt,
v3 direct K/V bank) is judged by the same refusal-rate and KL-preservation
metrics used by weight-space abliteration tools, so numbers compare directly.
"""
